"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const handshake_1 = require("../handshake");
describe("handshake utils", () => {
    it("creates a handshake message with the expected namespace marker", () => {
        const message = (0, handshake_1.createHandshakeMessage)(handshake_1.HANDSHAKE_REQUEST_TYPE);
        expect(message).toEqual({
            __iframeStorageHandshake: true,
            type: handshake_1.HANDSHAKE_REQUEST_TYPE,
        });
    });
    it("detects handshake messages regardless of type when not specified", () => {
        const request = (0, handshake_1.createHandshakeMessage)(handshake_1.HANDSHAKE_REQUEST_TYPE);
        const response = (0, handshake_1.createHandshakeMessage)(handshake_1.HANDSHAKE_RESPONSE_TYPE);
        expect((0, handshake_1.isHandshakeMessage)(request)).toBe(true);
        expect((0, handshake_1.isHandshakeMessage)(response)).toBe(true);
    });
    it("detects handshake messages only of the requested type when provided", () => {
        const request = (0, handshake_1.createHandshakeMessage)(handshake_1.HANDSHAKE_REQUEST_TYPE);
        const response = (0, handshake_1.createHandshakeMessage)(handshake_1.HANDSHAKE_RESPONSE_TYPE);
        expect((0, handshake_1.isHandshakeMessage)(request, handshake_1.HANDSHAKE_REQUEST_TYPE)).toBe(true);
        expect((0, handshake_1.isHandshakeMessage)(response, handshake_1.HANDSHAKE_REQUEST_TYPE)).toBe(false);
    });
    it("returns false when the payload is missing the handshake marker", () => {
        expect((0, handshake_1.isHandshakeMessage)({})).toBe(false);
        expect((0, handshake_1.isHandshakeMessage)(null)).toBe(false);
        expect((0, handshake_1.isHandshakeMessage)("not-a-message")).toBe(false);
    });
});
//# sourceMappingURL=handshake.test.js.map