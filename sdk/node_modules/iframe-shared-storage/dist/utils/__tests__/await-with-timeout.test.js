"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const await_with_timeout_1 = require("../await-with-timeout");
describe("awaitWithTimeout", () => {
    beforeEach(() => {
        jest.useFakeTimers();
    });
    afterEach(() => {
        jest.useRealTimers();
    });
    it("resolves with the underlying promise value when it settles before the timeout", async () => {
        const promise = new Promise((resolve) => setTimeout(() => resolve(42), 20));
        const resultPromise = (0, await_with_timeout_1.awaitWithTimeout)(promise, 100);
        jest.advanceTimersByTime(20);
        await expect(resultPromise).resolves.toBe(42);
    });
    it("returns the onTimeout result when the timeout fires first", async () => {
        const fallback = jest.fn().mockReturnValue("fallback");
        const neverResolves = new Promise(() => {
            /* intentionally empty */
        });
        const resultPromise = (0, await_with_timeout_1.awaitWithTimeout)(neverResolves, 50, fallback);
        jest.advanceTimersByTime(50);
        await expect(resultPromise).resolves.toBe("fallback");
        expect(fallback).toHaveBeenCalledTimes(1);
    });
    it("rejects with a timeout error when no onTimeout handler is provided", async () => {
        const neverResolves = new Promise(() => {
            /* intentionally empty */
        });
        const resultPromise = (0, await_with_timeout_1.awaitWithTimeout)(neverResolves, 25);
        jest.advanceTimersByTime(25);
        await expect(resultPromise).rejects.toThrow("Operation timed out.");
    });
});
//# sourceMappingURL=await-with-timeout.test.js.map